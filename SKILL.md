---
name: paper-figure-generate-skill
description: 为科研论文创建、重绘和精修可编辑的 SVG 插图，包括复杂方法框架、系统架构、多分支模型、算法流程和实验图。适用于包含嵌套模块、并行分支、共享组件、多类连接及反馈回路的论文绘图，强调关键文字、语义图标、低饱和配色和精细布局，支持独立 SVG 及 PNG、PDF 导出。
---

# 科研论文 SVG 绘图

以科研信息的准确表达为核心，制作结构清晰、文字精简、图标恰当、可继续编辑的论文插图。SVG 是交付主文件，PNG 和 PDF 由同一版本 SVG 导出。

设计规范与工具实现分离。此skill不要求某个操作系统、桌面应用、专属工具接口、浏览器品牌或预装运行时；按当前环境具备的SVG编写、渲染、导出和检查能力选择工具。随附脚本是可选适配器，不是唯一实现路径，缺少其依赖时使用等价工具并保持同一质量要求。

根据表达任务选择结构：流程图强调步骤与先后顺序；框架图强调系统组成、模块归属、内部机制和交互关系。复杂框架不能简单压缩成一排“图标 + 模块名”的流程卡片，也不能把所有连接都解释为时间顺序。

## 基本约束

- **先确定比例，再绘制。** 开始创建图形之前，写出明确的画布比例、模块比例、留白和字号规范。用户给定的版面、配色及参考图优先。
- **框架版式规整、紧凑。** 论文框架图默认采用矩形分区和明确的行列网格，同排分区顶底对齐、同行内容建立公共基线。整体形成完整的矩形视觉轮廓，避免孤立模块、阶梯状外沿和大块无用途空白；“方正”不要求画布为正方形，也不要求全部模块等宽。
- **视觉重量有主次。** 以方法机制建立视觉入口，分区轮廓、节点、图标与注释按角色分配重量；规整的等高分栏不等于每栏都铺同样醒目的底色、边框和大图标。
- **容器层级可辨。** 背景底色、边框颜色与粗细共同表达主区、子区和节点；按模块归属划分子框，不能把所有容器统一成白底淡线。具体参数与验收见 [底色、边框与子分区](references/visual-craft.md#底色边框与子分区)。
- **文字只承载关键信息。** 保留模块名称、必要动作、符号和关键数值；解释性段落交给图注或正文。不能为减少字数而删去影响科学含义的信息。
- **用图标帮助理解。** 为主要实体和操作选择有语义的图标，适量增加视觉细节；避免无关装饰、用图标替代无法辨识的专业概念或在每个节点重复塞图标。
- **颜色有意义。** 采用低饱和的填充色，用颜色区分角色、阶段或状态；同一含义跨面板保持一致。文字与关键线条保持足够对比度，不随背景一起淡化。
- **保持可编辑和可移植。** 使用真实 SVG 图元、分组和文本；图标内嵌，不能依赖外部本地文件。不能将整幅位图包进 SVG 后称其为可编辑图。
- **布局必须完整。** 连线有明确起止点，图元处于所属边框之内，文字、箭头和图标之间不发生非预期覆盖。
- **框内内容双轴居中。** 每个元素框中的单个元素或整组“文本＋图标”必须在内容区水平、垂直居中。多元素按组合的实际可见包围盒整体对齐，保留内部顺序与间距，不把各元素叠到同一中心；标题栏与主体区分别居中。排除背景、边框和外接连线后测量，详见 [组合内容居中](references/visual-craft.md#组合内容居中)。
- **迭代到没有已知缺陷。** 每轮检查实际渲染结果并修复问题，不能仅凭代码或“文件成功导出”宣称质量合格。

## 1. 明确图要表达什么

从用户提供的论文、方法描述、草图或数据中提取：

1. 一句话主旨：读者看完图应理解什么。
2. 必须出现的实体、步骤、输入输出及它们的关系。
3. 图类型、目标读者、标签语言、单栏或跨栏用途及输出格式。
4. 需要遵守的术语、数值、比较关系和参考风格。

分别识别内容来源与视觉参考。仅作效果参考的图片用于提取比例、密度、对齐、配色与边框层级；其中的组件、专业符号、分区数量和连接拓扑不自动成为当前图的内容或模板。只有用户指定重绘或将图片作为内容来源时，才据其提取科学实体与关系。

默认沿用论文或现有图的标签语言；未知时在绘图说明中声明假设。缺少会改变科学含义的信息时，提出简短问题；可以独立确定的布局与风格继续推进。不编造组件、实验数据、结果或不存在的关系。

用户明确要求试画、演示或检验 skill，而未提供论文时，可以自拟与所需复杂度匹配的方法示例，在绘图说明和交付中标明其为假想方法；不将它包装成真实研究贡献。

先列出节点及关系，至少区分 `起点 → 终点 / 条件 / 主路径或反馈路径`。有分支或回路时，明确分支条件与回归位置，再为它们分配空间；不要先排列卡片，再勉强补上回路线。

**复杂框架图**：当内容包含嵌套子系统、并行分支与融合、共享存储或模型、不同类型的连接、训练与推理分区、整体与局部放大等结构时，绘制前阅读 [复杂框架图规范](references/complex-framework.md)。先建立模块层级与关系模型，再选择布局；按实际结构启用相应规则，不以节点数作为唯一判断标准。

**高密度复杂图版**：需要在有限版面内组织较多实体、关系或局部细节，或用户反馈空白过多、结构松散、信息密度不足时，另读 [高密度复杂图版设计](references/dense-mechanisms.md)。按当前内容与阅读任务预算空间、选择布局和展开程度，不预设领域、组件类型或固定拓扑。

**论文框架版式**：框架图在确定比例前阅读 [规整版式与视觉设计](references/paper-layout.md)，先把模块树组织为对齐的分栏、分行或主区加辅助带，再进行连线布局。用户提供样例时，提取其分区、对齐、密度和图文组织规律，写入绘图说明。

**视觉设计与精修**：新绘或重绘框架，以及用户反馈“缺少质感”“太像模板”时，阅读 [视觉层级与细节设计](references/visual-craft.md)。先诊断具体的视觉问题，将参考规律转为角色参数，重点打磨核心机制；用实际尺度的前后对照检验效果，不能只换配色或增加特效。

对于实验数据图，用准确数据驱动坐标、刻度、误差范围和图例。可以使用本地绘图库生成 SVG，再统一样式；不得手调柱高或曲线来制造效果，不能用装饰图标干扰数据读取。

## 2. 先形成比例与布局说明

绘制前使用 [绘图说明模板](references/figure-brief.md) 记录决定。简单修改只更新受影响的项目，无需重新设计整图。

至少写明以下内容，并给出具体数值，不能只说“横向”“适中”或“留白充分”：

- **最终用途与宽高比**：目标物理宽度（已知时）、画布 `viewBox` 宽高及比例。
- **空间分配**：外边距、主模块宽高或比例、模块间隔、连接线走廊，以及它们的总和如何落在画布内。
- **视觉尺度**：标题、标签、注释的字号，图标尺寸，容器内边距，线宽、箭头和圆角规格。
- **阅读顺序**：从左到右、从上到下或分面板阅读；主路径与次要关系如何区分。
- **语义配色**：每种颜色代表什么，并规定填充、边框和文字颜色。

复杂框架还要确定各子系统的面积分配、内部子网格、标题带、端口及跨区域布线路径。先画容器、节点占位和主次连接的线框草稿，检查科学关系、公共对齐线、矩形外轮廓和空间密度，再细化内部机制、图标与配色。线框仍有明显空洞或散落模块时，先重排分区。

比例由论文版面和关系图决定，不默认套用演示文稿的 16:9。没有版面要求时，可声明以跨栏宽 `180 mm` 为设计假设，用 `1200` 单位画布宽度计算模块、连线与反馈通道，再确定高度。单栏图须按实际栏宽重新排版。

先规定最终物理字号，再换算 SVG 字号：`SVG 字号 = 目标 pt × 25.4 / 72 × viewBox 宽 / 成图宽 mm`。无期刊规范时，可从辅助标签 9–10 pt、主标签 11–13 pt 开始，并作实际尺度复核；这些是设计起点，不是所有期刊的要求。线宽、图标和间距同样随最终使用尺寸检查。

说明确定后直接开始绘制；若已有足够信息，无需将该步骤变成等待用户批准的关卡。随后如需改变比例，同步更新说明。

## 3. 选择图标与视觉语言

优先检索**用户当前项目根目录下的 `icons/`**；若与 skill 根目录不同，再检索本 skill 的 `icons/`。先用文件名、目录名及少量候选预览选取，避免一次读入整个图标库。

文件名只用于召回候选，不能证明图标语义。查看候选图标实际渲染，图标较多时生成带文件名的缩略图总览；在绘图说明记录“模块 → 来源 → 视觉含义”。例如名为 verify 的文件可能画的是代码终端，是否适用取决于当前图的语义。

- 先匹配语义，再比较轮廓、描边、填充风格和复杂度。整图尽量使用同一视觉风格。
- 优先复用已有 `.svg`，允许根据颜色语义调整填充与描边；保持图标比例，按实际视觉边界校正大小与居中。
- 将图标图元及其所需定义复制到最终 SVG 中；处理 ID、样式和引用冲突。具体操作见 [SVG 与导出规范](references/svg-and-export.md)。
- 图标缺失或不合适时，用简单的 SVG 几何图形绘制必要符号，或使用清晰的文字标签；在绘图说明中记录。无需为了凑数量加入图标。
- 遵守已有图标许可证与署名要求；有要求时随交付保留必要信息。

先确定一套紧凑的视觉规范，再重复使用。可采用下列低饱和起始色，但按实际含义分配，不能为了好看随机上色：

| 示例角色 | 填充 | 边框 / 图标 |
| --- | --- | --- |
| 输入、数据 | `#E8EFF5` | `#607F98` |
| 核心处理 | `#E7EFE9` | `#68816F` |
| 输出、产物 | `#F3EBDD` | `#9A8055` |
| 可选、辅助 | `#EEEAF3` | `#847696` |

正文可从 `#29323A`、背景从 `#FFFFFF` 开始。不能仅靠颜色表达类别；结合标签、图标、线型或位置，使灰度查看时仍可理解。

## 4. 构建 SVG

开始编码前阅读 [工具链用法](references/toolchain.md) 与 [SVG 与导出规范](references/svg-and-export.md)。

一套已验证的可选实现是：Python标准库 `xml.etree.ElementTree` 生成SVG和内嵌图标；Node.js + Sharp渲染PNG；Playwright + Chromium系列浏览器导出PDF并测量边界；Python、Poppler等辅助检查。其他能满足要求的SVG编辑器、绘图库或渲染器同样适用。布局由设计说明和生成代码决定，工具不会自动完成版式设计。

先确认可用的渲染与导出工具，必要时用一个包含代表性图标、文字和箭头的最小 SVG 验证工具链，再批量绘制。工具不可用时尽早选择替代路径；不要等整图完成后才发现无法导出。

按已确定的网格和尺寸建立布局参数，先放置模块和连接锚点，再放入图标与精简文本，最后处理图例及局部细节。布局参数集中管理，避免大量互不关联的坐标补丁。

- 使用有语义的 `<g id="…">` 组织面板、模块、图标、标签和连接线；文本默认保留为 `<text>` / `<tspan>`。
- 同级模块统一内边距、对齐方式、图标占位和标签基线。文字放不下时优先精简、换行或扩大空间，不任意缩小单个标签。
- 主路径保持明确的视觉优先级。优先通过位置与分组传递层级，避免过多嵌套边框、阴影及装饰。
- 框架图按模块语义选用内部图形，例如特征堆叠、候选集合、状态存储、编码与融合结构；已有图标用于提示实体，不代替必须展示的方法机制。外围组件可以简化，核心贡献需要足够的内部细节。
- 边从模块边界的明确锚点出入。让箭头、标签和圆角占用真实空间，为连接线预留通道。
- 连续关系优先用一条完整路径表示。转折和分支保持接合，交叉不代表连接时采用绕行或清晰区分；不得用背景色遮盖出假断口。
- 以视觉边界检查是否越界，包括描边、箭头尖端、图标的实际边界和多行文字。

## 5. 渲染、检查与精修

先用可用的XML解析器和引用检查方法检查结构，再将 SVG 实际渲染为 PNG 检视。有Python时，可运行 `python3 <skill目录>/scripts/check_svg.py <图.svg>`；退出码 `0/1/2` 分别表示支持范围内通过 / 有错误 / 需进一步检查，详见 [SVG 与导出规范](references/svg-and-export.md)。需要 PDF 时也检查导出的 PDF 页面，不能假定 SVG 正常就意味着 PDF 正常。

可直接使用随skill提供的 `scripts/render_png.cjs` 和 `scripts/export_browser.cjs` 完成PNG导出、浏览器截图、PDF及边界测量；命令参数、模块标记约定、报告范围与替代工具见 [工具链用法](references/toolchain.md)。测量结果绑定源SVG版本；修改后重新渲染与测量。

每轮检查：

1. **科学语义**：实体、方向、顺序、符号、数据与源材料一致。
2. **阅读负担**：主旨明确，文字只留关键内容，图标容易理解，主次关系清楚。
3. **比例与尺度**：整体符合布局说明；在最终论文显示尺寸下，最小标签仍可读。
4. **几何与连接**：无非预期重叠、溢出、裁切；逐框检查单元素或图文组合的水平、垂直居中；端口对齐，箭头连续，标签不压线。
5. **风格与细节**：配色语义一致；图标尺度、描边、字号、基线和间距协调。
6. **文件完整性**：SVG 可解析，无重复 ID、失效内部引用或外部资源依赖；导出格式没有缺字和样式漂移。

框架图还须通过 [版式验收](references/paper-layout.md#版式验收)：外轮廓、公共边线、标题带、空间密度与图文尺度都要检查。结构正确、没有碰撞，只能说明几何检查通过，不能单独证明版式已达到论文插图要求。

视觉精修按 [设计评审与停止条件](references/visual-craft.md#设计评审与停止条件) 记录语义功能、层级、细节、系统一致性和方法辨识度的具体证据。减少无意义的框与装饰时，保留真实端口和科学内容；不把稀有字体或统一极简样式作为质量前提。

复杂框架另按 [专项检查](references/complex-framework.md#复杂框架专项检查) 逐项核对模块归属、分支与融合、共享组件、不同类型的边及跨区域连接；沿关键路径逐边追踪，而不只检查单个框是否美观。

同时查看整图与关键局部放大图；必要时查看灰度效果。修复缺陷后重新渲染受影响版本，确保 SVG 与全部导出物一致。至少完成一次“渲染—检查—修正（有问题时）—复查”的闭环；存在已知缺陷就继续修复。没有新问题时结束，不为了轮数机械重画。

有浏览器或矢量工具可用时，测量渲染后的文字和图标边界，辅助发现越框及内边距不足；分开记录“静态引用检查”“几何测量”“视觉复查”的结果。几何测量须说明覆盖范围，不能把文字包围盒通过等同于全图无重叠。无缺陷的部分不做无依据重构。

若缺少渲染器或无法检查图像，明确记录尚未验证的项目与原因；不得宣称已完成视觉验收。

## 6. 交付

默认在用户项目的 `outputs/<figure-name>/` 下交付；用户已有输出目录约定时遵循约定：

- `<figure-name>.svg`：独立、可编辑的主文件。
- `<figure-name>.png`：用于预览或用户指定分辨率的输出。
- `<figure-name>.pdf`：用户需要时提供，尽量保留矢量内容。
- `figure-brief.md`：比例、颜色含义、关键假设、图标来源及简短检查记录。
- 生成脚本：若通过脚本生成，保留可复现版本；声明其依赖。最终 SVG 不依赖脚本或原始图标文件。

向用户展示最终预览和文件链接，简述图的表达逻辑及未解决的限制。迭代已有图片时保留前版或可恢复历史，明确本轮改动，避免留下多个无法分辨的“最终版”。
